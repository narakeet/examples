name: Make videos
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: videopuppet/build-video-github-action@test8
      id: download
      with:
        source-path: hello-world/script/source.yml
        github-token: ${{ secrets.GITHUB_TOKEN }}
        videopuppet-api-key: ${{ secrets.VIDEOPUPPET_API_KEY }}
        api-url: https://devapi.videopuppet.com/video/build
    - uses: actions/upload-artifact@v1
      with:
        name: video
        path: "${{ steps.download.outputs.video-file }}"
